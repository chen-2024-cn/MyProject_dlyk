server:
  port: 8089
  servlet:
    context-path: /

spring:
  servlet:
    multipart:
      # AI 附件导入上传限制（默认 1MB 不够 Excel 导入场景）
      max-file-size: 5MB
      max-request-size: 10MB
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/dlyk?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    hikari:
      maximum-pool-size: 30
      minimum-idle: 20
      connection-timeout: 5000
      idle-timeout: 0
      #???????????????8??
      max-lifetime: 18000000

  data:
    redis:
      host: 127.0.0.1
      port: 6379
      password: 123456
      database: 0
# 设置返回json时的时区和格式
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
    #spring AI
  ai:
    openai:
      api-key: 后续准备接入 ai
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7


mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# JWT验证安全密钥配置
jwt:
  secret: dY8300olWQ3345;1d<3w48

# LangChain4j custom properties
ai:
  chat:
    base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
    api-key: sk-13e16664d4f740e0a41e87fd06dfa8fa  # 默认极客测试 Key
    model-name: qwen3.7-max-2026-06-08

#定时任务的调度时间
project:
  task:
    cron: '*/3 * * * * *'
    delay: 180000

