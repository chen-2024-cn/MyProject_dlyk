# IDE
.idea/
.vscode/
*.iml

# Claude 内部文件
.claude/
.mcp.json

# 前端
front/node_modules/
front/dist/

# 后端
server/target/
server/.mvn/wrapper/maven-wrapper.jar
# 根目录构建产物（AI导出/沙箱产物曾被纳入版本库，target 在根也需忽略）
/target/

# 日志（运行期日志可能含敏感信息，禁止提交）
*.log

# 环境变量密钥文件（只允许 .env.example 入库，本地 .env 绝不提交）
.env
.env.local

# ---------------------------------------------------------------------------
# 数据库脚本：结构 + 假数据可入库；从真实库导出的全量快照禁止入库
# ---------------------------------------------------------------------------
# 现状（2026-09-11 结构与数据分离改造）：原全量 dump 已拆为两个文件——
#   · server/db/ApexSales.sql      纯建库建表结构（19 张表，不含任何数据），可安全入库；
#   · server/db/ApexSales_data.sql 随机生成的测试假数据（登录密码统一 123456，
#                                  客户姓名/手机/邮箱/地址均为虚构），不含真实 PII，可入库。
#   他人 clone 后依次执行这两个文件即可重建并填充测试库（必须先结构后数据）。
#
# 仍需忽略：任何从真实库导出的全量快照（mysqldump）可能含 t_user 的 BCrypt 密码哈希
# 与 t_clue 中客户真实 PII。本仓库为【公开仓库】，这类文件一旦提交即等同把整库登录
# 凭据与客户信息发到公网，且永久留在 git 历史里。故对疑似真实 dump 一律忽略——以后
# 导出真实快照时请命名为 *_dump.sql / dump_*.sql / *_backup.sql，切勿覆盖上面的纯结构文件：
server/db/*_dump.sql
server/db/dump_*.sql
server/db/*_backup.sql

# 系统
.DS_Store
Thumbs.db

CLAUDE.md